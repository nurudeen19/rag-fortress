Action items TODO

If the top match is above a confidence threshold (example: cosine similarity > 0.75), you’re usually safe with a small k.
If the top match is below that threshold (example: < 0.55), then it’s a weak hit, so increase k immediately.

setup quantized model for internal llm

Test to see if no errors are reported


Future file inclusions
external data sources api/web content
sql data sources
external files like s3 files etc



Add detailed readme for setup, and other scripts plus their configurations

Phase 1 - Basic CRUD (Conversation + Message models)
Phase 2 - Pagination + Caching
Phase 3 - Denormalized stats + Soft deletes
Phase 4 - Advanced features (search, categories, auto-titles)

