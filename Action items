Action items TODO

modularize file upload service if possible
dispatch ingestion tasks on app startup using settings from db
implement AUTH login/logout and the resets


setup quantized model for internal llm
setup llm router to switch llm based on security level of query
provide option to do a partial and full app setup
store custom config in db to be used in app startup for llm overrides from .env
setup manual history tracking per user session
always log security incidents on when users try to access restricted info

Test to see if no errors are reported


Future file inclusions
external data sources api/web content
sql data sources
external files like s3 files etc



Add detailed readme for setup, and other scripts plus their configurations

Phase 1 - Basic CRUD (Conversation + Message models)
Phase 2 - Pagination + Caching
Phase 3 - Denormalized stats + Soft deletes
Phase 4 - Advanced features (search, categories, auto-titles)

